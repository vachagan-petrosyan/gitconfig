# gitconfig
Clone repo into your home directory

```
git clone https://github.com/vachagan-petrosyan/gitconfig.git ~/
```

Go to cloned directory
```
cd ~/gitconfig
```

Copy gitconfig file into your home directory
```
cp gitconfig ~/.gitconfig
```

Reopen terminal to apply changes and use new settings.
